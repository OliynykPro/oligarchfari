import styled from "styled-components";

export const ApproveReportModalStyled = styled.div`
  div.buttons {
    margin-top: 3rem;
    text-align: right;

    button {
      :first-child {
        color: black;
        margin-right: 1rem;
        background-color: rgba(219, 219, 219, 0.4);
      }
    }
  }
`;
